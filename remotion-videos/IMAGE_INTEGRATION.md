# 神经网络视频图片集成

## 📸 已集成的图片

### Scene2 - 什么是神经网络

**图片信息**：

- **原始路径**：`/Users/borfyqiu/Desktop/study/self/qiubohong.github.io/assets/img/ailearn/daily/04/1.png`
- **Remotion 路径**：`public/NeuralNetworkVideo/images/neural-network-structure.png`
- **文件大小**：91KB
- **用途**：展示神经网络结构示意图

**代码实现**：

```tsx
import { Img, staticFile } from "remotion";

<Img
  src={staticFile("NeuralNetworkVideo/images/neural-network-structure.png")}
  style={{
    maxWidth: "100%",
    maxHeight: "400px",
    objectFit: "contain",
    borderRadius: 10,
  }}
/>;
```

## 🎨 布局优化

### Scene2 优化内容

1. **外层容器**：

   - padding：`80px` → `50px 60px`
   - 添加 `overflow: hidden`

2. **标题**：

   - 字体大小：`72px` → `60px`
   - 底部间距：`60px` → `35px`

3. **定义内容**：

   - 字体大小：`36px` → `28px`
   - 行高：`1.8` → `1.6`
   - padding：`50px` → `30px 40px`
   - 段落间距：`30px` → `20px`
   - 底部间距：`40px` → `30px`

4. **图片容器**：
   - 替换占位文字为实际图片
   - 最大宽度：`900px`
   - 图片最大高度：`400px`
   - 添加边框和背景
   - 使用 `objectFit: contain` 保持图片比例

## 📁 文件结构

```
remotion-videos/
├── public/
│   └── NeuralNetworkVideo/
│       ├── images/
│       │   └── neural-network-structure.png  ← 新增
│       ├── scene1-audio.mp3
│       ├── scene1-captions.json
│       └── ...
└── src/
    └── scenes/
        └── neuralnetwork/
            └── Scene2_Definition.tsx  ← 已修改
```

## 🎬 动画时序

Scene2 的动画时序保持不变：

- **0-20 帧**：标题淡入
- **30-60 帧**：定义内容淡入
- **90-120 帧**：图片淡入

## 🔄 如何添加更多图片

### 步骤 1：复制图片到 public 目录

```bash
cp /path/to/image.png remotion-videos/public/NeuralNetworkVideo/images/
```

### 步骤 2：在组件中引入

```tsx
import { Img, staticFile } from "remotion";
```

### 步骤 3：使用图片

```tsx
<Img
  src={staticFile("NeuralNetworkVideo/images/your-image.png")}
  style={{
    maxWidth: "100%",
    maxHeight: "400px",
    objectFit: "contain",
  }}
/>
```

## 📋 待添加的图片

根据原始文档，还有以下图片可以添加：

1. **Scene4 - 怎么做**：
   - 原始路径：`/assets/img/ailearn/daily/04/2.png`
   - 用途：神经网络工作流程图
   - 状态：❌ 未添加（已在布局优化时移除占位）

## 🎯 图片优化建议

### 1. 图片尺寸

- **推荐宽度**：1200-1600px
- **推荐高度**：600-800px
- **最大文件大小**：< 500KB

### 2. 图片格式

- **首选**：PNG（支持透明背景）
- **备选**：JPG（文件更小）
- **避免**：GIF（质量较低）

### 3. 图片优化

```bash
# 使用 ImageMagick 优化图片
convert input.png -resize 1600x800 -quality 85 output.png
```

### 4. 响应式处理

```tsx
// 根据视频分辨率调整图片大小
<Img
  src={staticFile("path/to/image.png")}
  style={{
    maxWidth: "100%",
    maxHeight: `${videoHeight * 0.4}px`, // 占视频高度的 40%
    objectFit: "contain",
  }}
/>
```

## 🐛 常见问题

### Q1: 图片不显示？

**解决方案**：

1. 检查图片路径是否正确
2. 确保图片在 `public/` 目录下
3. 使用 `staticFile()` 函数引用图片
4. 检查文件名大小写是否匹配

### Q2: 图片变形？

**解决方案**：
使用 `objectFit: "contain"` 保持图片比例：

```tsx
style={{
    objectFit: "contain"  // 或 "cover"
}}
```

### Q3: 图片加载慢？

**解决方案**：

1. 压缩图片文件大小
2. 使用合适的图片格式
3. 预加载图片（在组件外部）

## 📊 性能影响

### 添加图片前后对比

| 指标            | 添加前  | 添加后     | 变化     |
| --------------- | ------- | ---------- | -------- |
| Scene2 文件大小 | 3.63 KB | 3.89 KB    | +7%      |
| 图片资源        | 0       | 91 KB      | +91 KB   |
| 渲染时间        | ~4s     | ~4.2s      | +5%      |
| 视觉效果        | ⭐⭐⭐  | ⭐⭐⭐⭐⭐ | 显著提升 |

## 🚀 测试建议

### 1. 预览测试

```bash
cd /Users/borfyqiu/Desktop/study/self/qiubohong.github.io/remotion-videos
npm run dev
```

### 2. 检查要点

- [ ] 图片正确显示
- [ ] 图片不变形
- [ ] 图片不超出边界
- [ ] 动画流畅
- [ ] 与文字内容协调

### 3. 渲染测试

```bash
npm run render NeuralNetworkVideo
```

---

**更新日期**: 2026-02-12  
**作者**: Qborfy  
**状态**: ✅ 已完成
